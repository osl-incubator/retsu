{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Home","text":""},{"location":"#retsu","title":"Retsu","text":"<p>Retsu aims to wrap-up Celery in way to facilitate to create parallel and serial tasks</p> <ul> <li>License: BSD 3 Clause</li> <li>Documentation: https://retsu.github.io</li> </ul>"},{"location":"#features","title":"Features","text":"<ul> <li> <p>The security of our code: Bandit is a powerful tool that we use in our Python   project to ensure its security. This tool analyzes the code and detects   potential vulnerabilities. Some of the key features of Bandit are its ease of   use, its ability to integrate with other tools, and its support for multiple   Python versions. If you want to know about bandit you can check its   documentation.</p> </li> <li> <p>Finds unused code: Vulture is   useful for cleaning up and finding errors in large code bases in Python.</p> </li> <li> <p>Complexity of functions and modules: We use   McCabe to identify the complexity in our   Python code that may be difficult to maintain or understand. By identifying   complex code at the outset, we as developers can refactor it to make it easier   to maintain and understand. In summary, McCabe helps us to improve the quality   of our code and make it easier to maintain. If you would like to learn more   about McCabe and code complexity, you can visit   McCabe - Code Complexity Checker.   This tool is included with Flake8.</p> </li> <li> <p>TODO</p> </li> </ul>"},{"location":"#credits","title":"Credits","text":"<p>This package was created with Cookiecutter and the osl-incubator/scicookie project template.</p>"},{"location":"changelog/","title":"Release Notes","text":""},{"location":"changelog/#024-2024-08-16","title":"0.2.4 (2024-08-16)","text":""},{"location":"changelog/#bug-fixes","title":"Bug Fixes","text":"<ul> <li>Fix del and rename create_processes to setup (#18) (c4654d7)</li> </ul>"},{"location":"changelog/#023-2024-08-16","title":"0.2.3 (2024-08-16)","text":""},{"location":"changelog/#bug-fixes_1","title":"Bug Fixes","text":"<ul> <li>Fix function names and fix minor issues (#17) (f4cf368)</li> </ul>"},{"location":"changelog/#022-2024-08-14","title":"0.2.2 (2024-08-14)","text":""},{"location":"changelog/#bug-fixes_2","title":"Bug Fixes","text":"<ul> <li>Fix stopping workflow and refactor (#15) (5ab8f9b)</li> </ul>"},{"location":"changelog/#021-2024-08-12","title":"0.2.1 (2024-08-12)","text":""},{"location":"changelog/#bug-fixes_3","title":"Bug Fixes","text":"<ul> <li>Force process termination (#14) (0e4f088)</li> </ul>"},{"location":"changelog/#020-2024-06-29","title":"0.2.0 (2024-06-29)","text":""},{"location":"changelog/#features","title":"Features","text":"<ul> <li>Use redis for queues (#13) (750fb6b)</li> </ul>"},{"location":"changelog/#011-2024-06-20","title":"0.1.1 (2024-06-20)","text":""},{"location":"changelog/#bug-fixes_4","title":"Bug Fixes","text":"<ul> <li>Fix <code>get</code> command in the result attribute, and add <code>get_group_tasks</code> (#11) (ff29334)</li> </ul>"},{"location":"changelog/#010-2024-06-07","title":"0.1.0 (2024-06-07)","text":""},{"location":"changelog/#features_1","title":"Features","text":"<ul> <li>Add method create_task to the ProcessManager class (07379b8)</li> </ul>"},{"location":"changelog/#004-2024-05-31","title":"0.0.4 (2024-05-31)","text":""},{"location":"changelog/#bug-fixes_5","title":"Bug Fixes","text":"<ul> <li>Improve the task metadata handling (#7) (476e0b4)</li> </ul>"},{"location":"changelog/#003-2024-05-29","title":"0.0.3 (2024-05-29)","text":""},{"location":"changelog/#bug-fixes_6","title":"Bug Fixes","text":"<ul> <li>Fix initial issues with the structure and the example (#5) (7d520f9)</li> <li>wait for the celery task until the end of the execution. (#6) (f1ba586)</li> </ul>"},{"location":"changelog/#002-2024-05-28","title":"0.0.2 (2024-05-28)","text":""},{"location":"changelog/#bug-fixes_7","title":"Bug Fixes","text":"<ul> <li>Fix documentation build step (#3) (1244e73)</li> <li>Fix issue with semantic-release version pattern (#4) (6ce350a)</li> </ul>"},{"location":"contributing/","title":"Contributing","text":"<p>In order to be able to contribute, it is important that you understand the project layout.</p> <p>This project uses the src layout, which means that the package code is located at <code>./src/retsu</code>.</p> <p>For my information, check the official documentation: https://packaging.python.org/en/latest/discussions/src-layout-vs-flat-layout/</p> <p>In addition, you should know that to build our package we use Poetry, it's a Python package management tool that simplifies the process of building and publishing Python packages. It allows us to easily manage dependencies, virtual environments and package versions. Poetry also includes features such as dependency resolution, lock files and publishing to PyPI. Overall, Poetry streamlines the process of managing Python packages, making it easier for us to create and share our code with others.</p> <p>Contributions are welcome, and they are greatly appreciated! Every little bit helps, and credit will always be given.</p> <p>You can contribute in many ways:</p>"},{"location":"contributing/#types-of-contributions","title":"Types of Contributions","text":""},{"location":"contributing/#report-bugs","title":"Report Bugs","text":"<p>Report bugs at https://github.com/xmnlab/retsu.git/issues.</p> <p>If you are reporting a bug, please include:</p> <ul> <li>Your operating system name and version.</li> <li>Any details about your local setup that might be helpful in troubleshooting.</li> <li>Detailed steps to reproduce the bug.</li> </ul>"},{"location":"contributing/#fix-bugs","title":"Fix Bugs","text":"<p>Look through the GitHub issues for bugs. Anything tagged with \u201cbug\u201d and \u201chelp wanted\u201d is open to whoever wants to implement it.</p>"},{"location":"contributing/#implement-features","title":"Implement Features","text":"<p>Look through the GitHub issues for features. Anything tagged with \u201cenhancement\u201d and \u201chelp wanted\u201d is open to whoever wants to implement it.</p>"},{"location":"contributing/#write-documentation","title":"Write Documentation","text":"<p>Retsu could always use more documentation, whether as part of the official Retsu docs, in docstrings, or even on the web in blog posts, articles, and such.</p>"},{"location":"contributing/#submit-feedback","title":"Submit Feedback","text":"<p>The best way to send feedback is to file an issue at https://github.com/xmnlab/retsu.git/issues.</p> <p>If you are proposing a feature:</p> <ul> <li>Explain in detail how it would work.</li> <li>Keep the scope as narrow as possible, to make it easier to implement.</li> <li>Remember that this is a volunteer-driven project, and that contributions are   welcome :)</li> </ul>"},{"location":"contributing/#get-started","title":"Get Started!","text":"<p>Ready to contribute? Here\u2019s how to set up <code>retsu</code> for local development.</p> <ol> <li>Fork the <code>retsu</code> repo on GitHub.</li> <li>Clone your fork locally and change to the directory of your project:</li> </ol> <pre><code>$ git clone git@github.com:your_name_here/retsu.git\n$ cd retsu/\n</code></pre> <p>Also, create a remote to the upstream repository, you will need that later:</p> <pre><code>$ git remote add upstream https://github.com/xmnlab/retsu.git\n$ git fetch --all\n</code></pre> <ol> <li>Prepare and use virtual environment: If you don't have yet conda installed     in your machine, you can check the installation steps here:     conda-forge/miniforge?tab=readme-ov-file#download After     that, ensure that conda is already available in your terminal session and     run:</li> </ol> <pre><code>$ conda env create env create --file conda/dev.yaml\n$ conda activate retsu\n</code></pre> <p>Note: you can use <code>mamba env create</code> instead, if you have it already installed, in order to boost the installation step.</p> <ol> <li>Install the dependencies: Now, you can already install the dependencies for    the project:</li> </ol> <pre><code>$ poetry install\n</code></pre> <ol> <li><code>retsu</code> uses a set of <code>pre-commit</code> hooks to improve code quality. The hooks     can be installed locally using:</li> </ol> <pre><code>$ pre-commit install\n</code></pre> <p>This would run the checks every time a <code>git commit</code> is executed locally. Usually, the verification will only run on the files modified by that commit, but the verification can also be triggered for all the files using:</p> <pre><code>$ pre-commit run --all-files\n</code></pre> <p>If you would like to skip the failing checks and push the code for further discussion, use the <code>--no-verify</code> option with <code>git commit</code>.</p> <ol> <li>This project uses <code>pytest</code> as a testing tool. <code>pytest</code> is responsible for     testing the code, whose configuration is available in pyproject.toml.     Additionally, this project also uses <code>pytest-cov</code> to calculate the coverage     of these unit tests. For more information, check the section about tests     later in this document.</li> <li>Commit your changes and push your branch to GitHub::</li> </ol> <pre><code>$ git add .\n$ git commit -m \"Your detailed description of your changes.\"\"\n$ git push origin name-of-your-bugfix-or-feature\n</code></pre> <ol> <li>Submit a pull request through the GitHub website.</li> </ol>"},{"location":"contributing/#pull-request-guidelines","title":"Pull Request Guidelines","text":"<p>Before you submit a pull request, check that it meets these guidelines:</p> <ol> <li>The pull request should include tests.</li> <li>If the pull request adds functionality, the docs should be updated. Put your     new functionality into a function with a docstring, and add the feature to     the list in README.rst.</li> <li>The pull request should work for Python &gt;= 3.8.</li> </ol>"},{"location":"contributing/#running-tests-locally","title":"Running tests locally","text":"<p>The tests can be executed using the <code>test</code> dependencies of <code>retsu</code> in the following way:</p> <pre><code>$ python -m pytest\n</code></pre>"},{"location":"contributing/#running-tests-with-coverage-locally","title":"Running tests with coverage locally","text":"<p>The coverage value can be obtained while running the tests using <code>pytest-cov</code> in the following way:</p> <pre><code>$ python -m pytest --cov=retsu tests/\n</code></pre> <p>A much more detailed guide on testing with <code>pytest</code> is available here.</p>"},{"location":"contributing/#automation-tasks-with-makim","title":"Automation Tasks with Makim","text":"<p>This project uses <code>makim</code> as an automation tool. Please, check the <code>.makim.yaml</code> file to check all the tasks available or run:</p> <pre><code>$ makim --help\n</code></pre>"},{"location":"contributing/#release","title":"Release","text":"<p>This project uses semantic-release in order to cut a new release based on the commit-message.</p>"},{"location":"contributing/#commit-message-format","title":"Commit message format","text":"<p>semantic-release uses the commit messages to determine the consumer impact of changes in the codebase. Following formalized conventions for commit messages, semantic-release automatically determines the next semantic version number, generates a changelog and publishes the release.</p> <p>By default, semantic-release uses Angular Commit Message Conventions. The commit message format can be changed with the <code>preset</code> or <code>config</code> options_ of the @semantic-release/commit-analyzer and @semantic-release/release-notes-generator plugins.</p> <p>Tools such as commitizen or commitlint can be used to help contributors and enforce valid commit messages.</p> <p>The table below shows which commit message gets you which release type when <code>semantic-release</code> runs (using the default configuration):</p> Commit message Release type <code>fix(pencil): stop graphite breaking when pressure is applied</code> Fix Release <code>feat(pencil): add 'graphiteWidth' option</code> Feature Release <code>perf(pencil): remove graphiteWidth option</code> Chore <code>feat(pencil)!: The graphiteWidth option has been removed</code> Breaking Release <p>Note: For a breaking change release, uses <code>!</code> at the end of the message prefix.</p> <p>source: https://github.com/semantic-release/semantic-release/blob/master/README.md#commit-message-format</p> <p>As this project uses the <code>squash and merge</code> strategy, ensure to apply the commit message format to the PR's title.</p>"},{"location":"example/","title":"Retsu","text":"In\u00a0[\u00a0]: Copied! <pre>import retsu\n</pre> import retsu In\u00a0[\u00a0]: Copied! <pre>\n</pre>"},{"location":"example/#retsu","title":"Retsu\u00b6","text":"<p>Retsu is Python library that aims to do ...</p>"},{"location":"example/#getting-started","title":"Getting Started\u00b6","text":"<p>First, check our documentation about the installation.</p> <p>Now, let's import our library:</p>"},{"location":"installation/","title":"Installation","text":""},{"location":"installation/#stable-release","title":"Stable release","text":"<p>To install Retsu, run this command in your terminal:</p> <pre><code>$ pip install retsu\n</code></pre> <p>This is the preferred method to install Retsu, as it will always install the most recent stable release.</p> <p>If you don't have pip installed, this Python installation guide can guide you through the process.</p>"},{"location":"installation/#from-sources","title":"From sources","text":"<p>The sources for Retsu can be downloaded from the Github repo.</p> <p>You can either clone the public repository:</p> <pre><code>$ git clone https://github.com/xmnlab/retsu.git\n</code></pre> <p>Or download the tarball:</p> <pre><code>$ curl -OJL https://github.com/xmnlab/retsu.git/tarball/main\n</code></pre> <p>Once you have a copy of the source, you can install it with:</p> <pre><code>$ poetry install\n</code></pre>"},{"location":"api/","title":"Index","text":""},{"location":"api/#retsu","title":"retsu","text":"<p>Retsu.</p>"},{"location":"api/#retsu.MultiProcess","title":"MultiProcess","text":"<pre><code>MultiProcess(workers: int = 1)\n</code></pre> <p>               Bases: <code>Process</code></p> <p>Initialize a parallel process object.</p> Source code in <code>src/retsu/core.py</code> <pre><code>def __init__(self, workers: int = 1) -&gt; None:\n    \"\"\"Initialize MultiProcess.\"\"\"\n    if workers &lt;= 1:\n        raise Exception(\"MultiProcess should have more than 1 worker.\")\n\n    super().__init__(workers=workers)\n</code></pre>"},{"location":"api/#retsu.MultiProcess.prepare_process","title":"prepare_process","text":"<pre><code>prepare_process(data: dict[str, Any]) -&gt; None\n</code></pre> <p>Call the process with the necessary arguments.</p> Source code in <code>src/retsu/core.py</code> <pre><code>def prepare_process(self, data: dict[str, Any]) -&gt; None:\n    \"\"\"Call the process with the necessary arguments.\"\"\"\n    task_id = data.pop(\"task_id\")\n    self.result.metadata.update(task_id, \"status\", \"running\")\n    result = self.process(\n        *data[\"args\"],\n        task_id=task_id,\n        **data[\"kwargs\"],\n    )\n    self.result.save(task_id, result)\n    self.result.metadata.update(task_id, \"status\", \"completed\")\n</code></pre>"},{"location":"api/#retsu.MultiProcess.process","title":"process  <code>abstractmethod</code>","text":"<pre><code>process(*args, task_id: str, **kwargs) -&gt; Any\n</code></pre> <p>Define the process to be executed.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@abstractmethod\ndef process(self, *args, task_id: str, **kwargs) -&gt; Any:  # type: ignore\n    \"\"\"Define the process to be executed.\"\"\"\n    raise Exception(\"`process` not implemented yet.\")\n</code></pre>"},{"location":"api/#retsu.MultiProcess.request","title":"request","text":"<pre><code>request(*args, **kwargs) -&gt; str\n</code></pre> <p>Feed the queue with data from the request for the process.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef request(self, *args, **kwargs) -&gt; str:  # type: ignore\n    \"\"\"Feed the queue with data from the request for the process.\"\"\"\n    task_id = uuid4().hex\n    metadata = {\n        \"status\": \"starting\",\n        \"created_at\": datetime.now().isoformat(),\n        \"updated_at\": datetime.now().isoformat(),\n    }\n    self.result.create(task_id, metadata)\n    self.queue_in.put(\n        {\n            \"task_id\": task_id,\n            \"args\": args,\n            \"kwargs\": kwargs,\n        },\n    )\n    return task_id\n</code></pre>"},{"location":"api/#retsu.MultiProcess.run","title":"run","text":"<pre><code>run() -&gt; None\n</code></pre> <p>Run the process with data from the queue.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef run(self) -&gt; None:\n    \"\"\"Run the process with data from the queue.\"\"\"\n    while self.active:\n        data = self.queue_in.get()\n        self.prepare_process(data)\n</code></pre>"},{"location":"api/#retsu.MultiProcess.start","title":"start","text":"<pre><code>start() -&gt; None\n</code></pre> <p>Start processes.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef start(self) -&gt; None:\n    \"\"\"Start processes.\"\"\"\n    logging.info(f\"Starting process {self.__class__.__name__}\")\n    for _ in range(self.workers):\n        p = mp.Process(target=self.run)\n        p.start()\n        self.processes.append(p)\n</code></pre>"},{"location":"api/#retsu.MultiProcess.stop","title":"stop","text":"<pre><code>stop() -&gt; None\n</code></pre> <p>Stop processes.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef stop(self) -&gt; None:\n    \"\"\"Stop processes.\"\"\"\n    logging.info(f\"Stopping process {self.__class__.__name__}\")\n    if not self.active:\n        return\n\n    self.active = False\n\n    for i in range(self.workers):\n        p = self.processes[i]\n        p.terminate()\n        p.join(timeout=1)\n</code></pre>"},{"location":"api/#retsu.Process","title":"Process","text":"<pre><code>Process(workers: int = 1)\n</code></pre> <p>Main class for handling a process.</p> Source code in <code>src/retsu/core.py</code> <pre><code>def __init__(self, workers: int = 1) -&gt; None:\n    \"\"\"Initialize a process object.\"\"\"\n    _klass = self.__class__\n    queue_in_name = f\"{_klass.__module__}.{_klass.__qualname__}\"\n\n    self._client = redis.Redis(\n        **get_redis_queue_config(),  # type: ignore\n        decode_responses=False,\n    )\n    self.active = True\n    self.workers = workers\n    self.result: ResultProcessManager = create_result_task_manager()\n    self.queue_in = RedisRetsuQueue(queue_in_name)\n    self.processes: list[mp.Process] = []\n</code></pre>"},{"location":"api/#retsu.Process.prepare_process","title":"prepare_process","text":"<pre><code>prepare_process(data: dict[str, Any]) -&gt; None\n</code></pre> <p>Call the process with the necessary arguments.</p> Source code in <code>src/retsu/core.py</code> <pre><code>def prepare_process(self, data: dict[str, Any]) -&gt; None:\n    \"\"\"Call the process with the necessary arguments.\"\"\"\n    task_id = data.pop(\"task_id\")\n    self.result.metadata.update(task_id, \"status\", \"running\")\n    result = self.process(\n        *data[\"args\"],\n        task_id=task_id,\n        **data[\"kwargs\"],\n    )\n    self.result.save(task_id, result)\n    self.result.metadata.update(task_id, \"status\", \"completed\")\n</code></pre>"},{"location":"api/#retsu.Process.process","title":"process  <code>abstractmethod</code>","text":"<pre><code>process(*args, task_id: str, **kwargs) -&gt; Any\n</code></pre> <p>Define the process to be executed.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@abstractmethod\ndef process(self, *args, task_id: str, **kwargs) -&gt; Any:  # type: ignore\n    \"\"\"Define the process to be executed.\"\"\"\n    raise Exception(\"`process` not implemented yet.\")\n</code></pre>"},{"location":"api/#retsu.Process.request","title":"request","text":"<pre><code>request(*args, **kwargs) -&gt; str\n</code></pre> <p>Feed the queue with data from the request for the process.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef request(self, *args, **kwargs) -&gt; str:  # type: ignore\n    \"\"\"Feed the queue with data from the request for the process.\"\"\"\n    task_id = uuid4().hex\n    metadata = {\n        \"status\": \"starting\",\n        \"created_at\": datetime.now().isoformat(),\n        \"updated_at\": datetime.now().isoformat(),\n    }\n    self.result.create(task_id, metadata)\n    self.queue_in.put(\n        {\n            \"task_id\": task_id,\n            \"args\": args,\n            \"kwargs\": kwargs,\n        },\n    )\n    return task_id\n</code></pre>"},{"location":"api/#retsu.Process.run","title":"run","text":"<pre><code>run() -&gt; None\n</code></pre> <p>Run the process with data from the queue.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef run(self) -&gt; None:\n    \"\"\"Run the process with data from the queue.\"\"\"\n    while self.active:\n        data = self.queue_in.get()\n        self.prepare_process(data)\n</code></pre>"},{"location":"api/#retsu.Process.start","title":"start","text":"<pre><code>start() -&gt; None\n</code></pre> <p>Start processes.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef start(self) -&gt; None:\n    \"\"\"Start processes.\"\"\"\n    logging.info(f\"Starting process {self.__class__.__name__}\")\n    for _ in range(self.workers):\n        p = mp.Process(target=self.run)\n        p.start()\n        self.processes.append(p)\n</code></pre>"},{"location":"api/#retsu.Process.stop","title":"stop","text":"<pre><code>stop() -&gt; None\n</code></pre> <p>Stop processes.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef stop(self) -&gt; None:\n    \"\"\"Stop processes.\"\"\"\n    logging.info(f\"Stopping process {self.__class__.__name__}\")\n    if not self.active:\n        return\n\n    self.active = False\n\n    for i in range(self.workers):\n        p = self.processes[i]\n        p.terminate()\n        p.join(timeout=1)\n</code></pre>"},{"location":"api/#retsu.ProcessManager","title":"ProcessManager","text":"<pre><code>ProcessManager()\n</code></pre> <p>Manage tasks.</p> Source code in <code>src/retsu/core.py</code> <pre><code>def __init__(self) -&gt; None:\n    \"\"\"Create a list of retsu tasks.\"\"\"\n    self.tasks: dict[str, Process] = {}\n</code></pre>"},{"location":"api/#retsu.ProcessManager.get_process","title":"get_process","text":"<pre><code>get_process(name: str) -&gt; Optional[Process]\n</code></pre> <p>Get a process with the given name.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef get_process(self, name: str) -&gt; Optional[Process]:\n    \"\"\"Get a process with the given name.\"\"\"\n    return self.tasks.get(name)\n</code></pre>"},{"location":"api/#retsu.ProcessManager.setup","title":"setup","text":"<pre><code>setup() -&gt; None\n</code></pre> <p>Get a process with the given name.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef setup(self) -&gt; None:\n    \"\"\"Get a process with the given name.\"\"\"\n    if self.tasks:\n        return\n\n    warnings.warn(\n        \"`self.tasks` is empty. Override `setup` and create \"\n        \"`self.tasks` with the proper tasks.\"\n    )\n</code></pre>"},{"location":"api/#retsu.ProcessManager.start","title":"start","text":"<pre><code>start() -&gt; None\n</code></pre> <p>Start tasks.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef start(self) -&gt; None:\n    \"\"\"Start tasks.\"\"\"\n    if not self.tasks:\n        self.setup()\n\n    for task_name, process in self.tasks.items():\n        process.start()\n</code></pre>"},{"location":"api/#retsu.ProcessManager.stop","title":"stop","text":"<pre><code>stop() -&gt; None\n</code></pre> <p>Stop tasks.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef stop(self) -&gt; None:\n    \"\"\"Stop tasks.\"\"\"\n    if not self.tasks:\n        warnings.warn(\"There is no tasks to be stopped.\")\n        return\n\n    for task_name, process in self.tasks.items():\n        process.stop()\n</code></pre>"},{"location":"api/#retsu.ResultProcessManager","title":"ResultProcessManager","text":"<pre><code>ResultProcessManager(host: str = 'localhost', port: int = 6379, db: int = 0)\n</code></pre> <p>Manage the result and metadata from tasks.</p> Source code in <code>src/retsu/results.py</code> <pre><code>def __init__(\n    self, host: str = \"localhost\", port: int = 6379, db: int = 0\n) -&gt; None:\n    \"\"\"Initialize ResultProcessManager.\"\"\"\n    self.client = redis.Redis(\n        host=host, port=port, db=db, decode_responses=False\n    )\n    self.metadata = ProcessMetadataManager(self.client)\n</code></pre>"},{"location":"api/#retsu.ResultProcessManager.create","title":"create","text":"<pre><code>create(task_id: str, metadata: dict[str, Any]) -&gt; None\n</code></pre> <p>Create a new metadata for a given process.</p> Source code in <code>src/retsu/results.py</code> <pre><code>def create(self, task_id: str, metadata: dict[str, Any]) -&gt; None:\n    \"\"\"Create a new metadata for a given process.\"\"\"\n    self.metadata.create(task_id, metadata)\n</code></pre>"},{"location":"api/#retsu.ResultProcessManager.get","title":"get","text":"<pre><code>get(task_id: str, timeout: Optional[int] = None) -&gt; Any\n</code></pre> <p>Get the result for a given process.</p> Source code in <code>src/retsu/results.py</code> <pre><code>def get(self, task_id: str, timeout: Optional[int] = None) -&gt; Any:\n    \"\"\"Get the result for a given process.\"\"\"\n    time_step = 0.5\n    if timeout:\n        timeout_countdown = float(timeout)\n        while self.status(task_id) != \"completed\":\n            sleep(time_step)\n            timeout_countdown -= time_step\n            if timeout_countdown &lt;= 0:\n                status = self.status(task_id)\n                raise Exception(\n                    \"Timeout(get): Process result is not ready yet. \"\n                    f\"Process status: {status}\"\n                )\n\n    elif self.status(task_id) != \"completed\":\n        status = self.status(task_id)\n        raise Exception(\n            \"Timeout(get): Process result is not ready yet. \"\n            f\"Process status: {status}\"\n        )\n    result = self.metadata.get(task_id, \"result\")\n    return pickle.loads(result) if result else result\n</code></pre>"},{"location":"api/#retsu.ResultProcessManager.load","title":"load","text":"<pre><code>load(task_id: str) -&gt; dict[str, Any]\n</code></pre> <p>Load the whole metadata for a given process.</p> Source code in <code>src/retsu/results.py</code> <pre><code>def load(self, task_id: str) -&gt; dict[str, Any]:\n    \"\"\"Load the whole metadata for a given process.\"\"\"\n    return self.metadata.get_all(task_id)\n</code></pre>"},{"location":"api/#retsu.ResultProcessManager.save","title":"save","text":"<pre><code>save(task_id: str, result: Any) -&gt; None\n</code></pre> <p>Save the result for a given process.</p> Source code in <code>src/retsu/results.py</code> <pre><code>def save(self, task_id: str, result: Any) -&gt; None:\n    \"\"\"Save the result for a given process.\"\"\"\n    self.metadata.update(task_id, \"result\", pickle.dumps(result))\n</code></pre>"},{"location":"api/#retsu.ResultProcessManager.status","title":"status","text":"<pre><code>status(task_id: str) -&gt; str\n</code></pre> <p>Get the status for a given process.</p> Source code in <code>src/retsu/results.py</code> <pre><code>def status(self, task_id: str) -&gt; str:\n    \"\"\"Get the status for a given process.\"\"\"\n    status = self.metadata.get(task_id, \"status\")\n    return status.decode(\"utf8\")\n</code></pre>"},{"location":"api/#retsu.SingleProcess","title":"SingleProcess","text":"<pre><code>SingleProcess(workers: int = 1)\n</code></pre> <p>               Bases: <code>Process</code></p> <p>Single Process class.</p> Source code in <code>src/retsu/core.py</code> <pre><code>def __init__(self, workers: int = 1) -&gt; None:\n    \"\"\"Initialize a serial process object.\"\"\"\n    if workers != 1:\n        warnings.warn(\n            \"SingleProcess should have just 1 worker. \"\n            \"Switching automatically to 1 ...\"\n        )\n        workers = 1\n    super().__init__(workers=workers)\n</code></pre>"},{"location":"api/#retsu.SingleProcess.prepare_process","title":"prepare_process","text":"<pre><code>prepare_process(data: dict[str, Any]) -&gt; None\n</code></pre> <p>Call the process with the necessary arguments.</p> Source code in <code>src/retsu/core.py</code> <pre><code>def prepare_process(self, data: dict[str, Any]) -&gt; None:\n    \"\"\"Call the process with the necessary arguments.\"\"\"\n    task_id = data.pop(\"task_id\")\n    self.result.metadata.update(task_id, \"status\", \"running\")\n    result = self.process(\n        *data[\"args\"],\n        task_id=task_id,\n        **data[\"kwargs\"],\n    )\n    self.result.save(task_id, result)\n    self.result.metadata.update(task_id, \"status\", \"completed\")\n</code></pre>"},{"location":"api/#retsu.SingleProcess.process","title":"process  <code>abstractmethod</code>","text":"<pre><code>process(*args, task_id: str, **kwargs) -&gt; Any\n</code></pre> <p>Define the process to be executed.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@abstractmethod\ndef process(self, *args, task_id: str, **kwargs) -&gt; Any:  # type: ignore\n    \"\"\"Define the process to be executed.\"\"\"\n    raise Exception(\"`process` not implemented yet.\")\n</code></pre>"},{"location":"api/#retsu.SingleProcess.request","title":"request","text":"<pre><code>request(*args, **kwargs) -&gt; str\n</code></pre> <p>Feed the queue with data from the request for the process.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef request(self, *args, **kwargs) -&gt; str:  # type: ignore\n    \"\"\"Feed the queue with data from the request for the process.\"\"\"\n    task_id = uuid4().hex\n    metadata = {\n        \"status\": \"starting\",\n        \"created_at\": datetime.now().isoformat(),\n        \"updated_at\": datetime.now().isoformat(),\n    }\n    self.result.create(task_id, metadata)\n    self.queue_in.put(\n        {\n            \"task_id\": task_id,\n            \"args\": args,\n            \"kwargs\": kwargs,\n        },\n    )\n    return task_id\n</code></pre>"},{"location":"api/#retsu.SingleProcess.run","title":"run","text":"<pre><code>run() -&gt; None\n</code></pre> <p>Run the process with data from the queue.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef run(self) -&gt; None:\n    \"\"\"Run the process with data from the queue.\"\"\"\n    while self.active:\n        data = self.queue_in.get()\n        self.prepare_process(data)\n</code></pre>"},{"location":"api/#retsu.SingleProcess.start","title":"start","text":"<pre><code>start() -&gt; None\n</code></pre> <p>Start processes.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef start(self) -&gt; None:\n    \"\"\"Start processes.\"\"\"\n    logging.info(f\"Starting process {self.__class__.__name__}\")\n    for _ in range(self.workers):\n        p = mp.Process(target=self.run)\n        p.start()\n        self.processes.append(p)\n</code></pre>"},{"location":"api/#retsu.SingleProcess.stop","title":"stop","text":"<pre><code>stop() -&gt; None\n</code></pre> <p>Stop processes.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef stop(self) -&gt; None:\n    \"\"\"Stop processes.\"\"\"\n    logging.info(f\"Stopping process {self.__class__.__name__}\")\n    if not self.active:\n        return\n\n    self.active = False\n\n    for i in range(self.workers):\n        p = self.processes[i]\n        p.terminate()\n        p.join(timeout=1)\n</code></pre>"},{"location":"api/#retsu.get_version","title":"get_version","text":"<pre><code>get_version() -&gt; str\n</code></pre> <p>Return the program version.</p> Source code in <code>src/retsu/__init__.py</code> <pre><code>def get_version() -&gt; str:\n    \"\"\"Return the program version.\"\"\"\n    try:\n        return importlib_metadata.version(__name__)\n    except importlib_metadata.PackageNotFoundError:  # pragma: no cover\n        return \"0.2.4\"  # semantic-release\n</code></pre>"},{"location":"api/SUMMARY/","title":"SUMMARY","text":"<ul> <li> retsu<ul> <li> celery</li> <li> core</li> <li> plugins<ul> <li> django</li> </ul> </li> <li> queues</li> <li> results</li> </ul> </li> </ul>"},{"location":"api/celery/","title":"<code class=\"doc-symbol doc-symbol-nav doc-symbol-module\"></code> celery","text":""},{"location":"api/celery/#retsu.celery","title":"celery","text":"<p>Retsu tasks with celery.</p>"},{"location":"api/celery/#retsu.celery.CeleryProcess","title":"CeleryProcess","text":"<p>Celery Process class.</p>"},{"location":"api/celery/#retsu.celery.CeleryProcess.get_chain_tasks","title":"get_chain_tasks","text":"<pre><code>get_chain_tasks(*args, **kwargs) -&gt; list[Signature]\n</code></pre> <p>Run tasks with chain.</p> Source code in <code>src/retsu/celery.py</code> <pre><code>def get_chain_tasks(  # type: ignore\n    self, *args, **kwargs\n) -&gt; list[celery.Signature]:\n    \"\"\"Run tasks with chain.\"\"\"\n    chain_tasks: list[celery.Signature] = []\n    return chain_tasks\n</code></pre>"},{"location":"api/celery/#retsu.celery.CeleryProcess.get_chord_tasks","title":"get_chord_tasks","text":"<pre><code>get_chord_tasks(*args, **kwargs) -&gt; tuple[list[Signature], Optional[Signature]]\n</code></pre> <p>Run tasks with chord.</p> Return Source code in <code>src/retsu/celery.py</code> <pre><code>def get_chord_tasks(  # type: ignore\n    self, *args, **kwargs\n) -&gt; tuple[list[celery.Signature], Optional[celery.Signature]]:\n    \"\"\"\n    Run tasks with chord.\n\n    Return\n    ------\n    tuple:\n        list of tasks for the chord, and the task to be used as a callback\n    \"\"\"\n    chord_tasks: list[celery.Signature] = []\n    callback_task = None\n    return (chord_tasks, callback_task)\n</code></pre>"},{"location":"api/celery/#retsu.celery.CeleryProcess.get_group_tasks","title":"get_group_tasks","text":"<pre><code>get_group_tasks(*args, **kwargs) -&gt; list[Signature]\n</code></pre> <p>Run tasks with group.</p> Return Source code in <code>src/retsu/celery.py</code> <pre><code>def get_group_tasks(  # type: ignore\n    self, *args, **kwargs\n) -&gt; list[celery.Signature]:\n    \"\"\"\n    Run tasks with group.\n\n    Return\n    ------\n    tuple:\n        list of tasks for the chord, and the task to be used as a callback\n    \"\"\"\n    group_tasks: list[celery.Signature] = []\n    return group_tasks\n</code></pre>"},{"location":"api/celery/#retsu.celery.CeleryProcess.process","title":"process","text":"<pre><code>process(*args, task_id: str, **kwargs) -&gt; Any\n</code></pre> <p>Define the process to be executed.</p> Source code in <code>src/retsu/celery.py</code> <pre><code>def process(self, *args, task_id: str, **kwargs) -&gt; Any:  # type: ignore\n    \"\"\"Define the process to be executed.\"\"\"\n    chord_tasks, chord_callback = self.get_chord_tasks(\n        *args,\n        task_id=task_id,\n        **kwargs,\n    )\n    group_tasks = self.get_group_tasks(\n        *args,\n        task_id=task_id,\n        **kwargs,\n    )\n    chain_tasks = self.get_chain_tasks(\n        *args,\n        task_id=task_id,\n        **kwargs,\n    )\n\n    # start the tasks\n    if chord_tasks:\n        workflow_chord = chord(chord_tasks, chord_callback)\n        promise_chord = workflow_chord.apply_async()\n\n    if group_tasks:\n        workflow_group = group(group_tasks)\n        promise_group = workflow_group.apply_async()\n\n    if chain_tasks:\n        workflow_chain = chain(chord_tasks)\n        promise_chain = workflow_chain.apply_async()\n\n    # wait for the tasks\n    results: list[Any] = []\n    if chord_tasks:\n        chord_result = promise_chord.get()\n        if isinstance(chord_result, list):\n            results.extend(chord_result)\n        else:\n            results.append(chord_result)\n\n    if group_tasks:\n        group_result = promise_group.get()\n        if isinstance(group_result, list):\n            results.extend(group_result)\n        else:\n            results.append(group_result)\n\n    if chain_tasks:\n        chain_result = promise_chain.get()\n\n        if isinstance(chain_result, list):\n            results.extend(chain_result)\n        else:\n            results.append(chain_result)\n\n    return results\n</code></pre>"},{"location":"api/celery/#retsu.celery.MultiCeleryProcess","title":"MultiCeleryProcess","text":"<pre><code>MultiCeleryProcess(workers: int = 1)\n</code></pre> <p>               Bases: <code>CeleryProcess</code>, <code>MultiProcess</code></p> <p>Multi Process for Celery.</p> Source code in <code>src/retsu/core.py</code> <pre><code>def __init__(self, workers: int = 1) -&gt; None:\n    \"\"\"Initialize MultiProcess.\"\"\"\n    if workers &lt;= 1:\n        raise Exception(\"MultiProcess should have more than 1 worker.\")\n\n    super().__init__(workers=workers)\n</code></pre>"},{"location":"api/celery/#retsu.celery.SingleCeleryProcess","title":"SingleCeleryProcess","text":"<pre><code>SingleCeleryProcess(workers: int = 1)\n</code></pre> <p>               Bases: <code>CeleryProcess</code>, <code>SingleProcess</code></p> <p>Single Process for Celery.</p> Source code in <code>src/retsu/core.py</code> <pre><code>def __init__(self, workers: int = 1) -&gt; None:\n    \"\"\"Initialize a serial process object.\"\"\"\n    if workers != 1:\n        warnings.warn(\n            \"SingleProcess should have just 1 worker. \"\n            \"Switching automatically to 1 ...\"\n        )\n        workers = 1\n    super().__init__(workers=workers)\n</code></pre>"},{"location":"api/core/","title":"<code class=\"doc-symbol doc-symbol-nav doc-symbol-module\"></code> core","text":""},{"location":"api/core/#retsu.core","title":"core","text":"<p>Retsu core classes.</p>"},{"location":"api/core/#retsu.core.MultiProcess","title":"MultiProcess","text":"<pre><code>MultiProcess(workers: int = 1)\n</code></pre> <p>               Bases: <code>Process</code></p> <p>Initialize a parallel process object.</p> Source code in <code>src/retsu/core.py</code> <pre><code>def __init__(self, workers: int = 1) -&gt; None:\n    \"\"\"Initialize MultiProcess.\"\"\"\n    if workers &lt;= 1:\n        raise Exception(\"MultiProcess should have more than 1 worker.\")\n\n    super().__init__(workers=workers)\n</code></pre>"},{"location":"api/core/#retsu.core.MultiProcess.prepare_process","title":"prepare_process","text":"<pre><code>prepare_process(data: dict[str, Any]) -&gt; None\n</code></pre> <p>Call the process with the necessary arguments.</p> Source code in <code>src/retsu/core.py</code> <pre><code>def prepare_process(self, data: dict[str, Any]) -&gt; None:\n    \"\"\"Call the process with the necessary arguments.\"\"\"\n    task_id = data.pop(\"task_id\")\n    self.result.metadata.update(task_id, \"status\", \"running\")\n    result = self.process(\n        *data[\"args\"],\n        task_id=task_id,\n        **data[\"kwargs\"],\n    )\n    self.result.save(task_id, result)\n    self.result.metadata.update(task_id, \"status\", \"completed\")\n</code></pre>"},{"location":"api/core/#retsu.core.MultiProcess.process","title":"process  <code>abstractmethod</code>","text":"<pre><code>process(*args, task_id: str, **kwargs) -&gt; Any\n</code></pre> <p>Define the process to be executed.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@abstractmethod\ndef process(self, *args, task_id: str, **kwargs) -&gt; Any:  # type: ignore\n    \"\"\"Define the process to be executed.\"\"\"\n    raise Exception(\"`process` not implemented yet.\")\n</code></pre>"},{"location":"api/core/#retsu.core.MultiProcess.request","title":"request","text":"<pre><code>request(*args, **kwargs) -&gt; str\n</code></pre> <p>Feed the queue with data from the request for the process.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef request(self, *args, **kwargs) -&gt; str:  # type: ignore\n    \"\"\"Feed the queue with data from the request for the process.\"\"\"\n    task_id = uuid4().hex\n    metadata = {\n        \"status\": \"starting\",\n        \"created_at\": datetime.now().isoformat(),\n        \"updated_at\": datetime.now().isoformat(),\n    }\n    self.result.create(task_id, metadata)\n    self.queue_in.put(\n        {\n            \"task_id\": task_id,\n            \"args\": args,\n            \"kwargs\": kwargs,\n        },\n    )\n    return task_id\n</code></pre>"},{"location":"api/core/#retsu.core.MultiProcess.run","title":"run","text":"<pre><code>run() -&gt; None\n</code></pre> <p>Run the process with data from the queue.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef run(self) -&gt; None:\n    \"\"\"Run the process with data from the queue.\"\"\"\n    while self.active:\n        data = self.queue_in.get()\n        self.prepare_process(data)\n</code></pre>"},{"location":"api/core/#retsu.core.MultiProcess.start","title":"start","text":"<pre><code>start() -&gt; None\n</code></pre> <p>Start processes.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef start(self) -&gt; None:\n    \"\"\"Start processes.\"\"\"\n    logging.info(f\"Starting process {self.__class__.__name__}\")\n    for _ in range(self.workers):\n        p = mp.Process(target=self.run)\n        p.start()\n        self.processes.append(p)\n</code></pre>"},{"location":"api/core/#retsu.core.MultiProcess.stop","title":"stop","text":"<pre><code>stop() -&gt; None\n</code></pre> <p>Stop processes.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef stop(self) -&gt; None:\n    \"\"\"Stop processes.\"\"\"\n    logging.info(f\"Stopping process {self.__class__.__name__}\")\n    if not self.active:\n        return\n\n    self.active = False\n\n    for i in range(self.workers):\n        p = self.processes[i]\n        p.terminate()\n        p.join(timeout=1)\n</code></pre>"},{"location":"api/core/#retsu.core.Process","title":"Process","text":"<pre><code>Process(workers: int = 1)\n</code></pre> <p>Main class for handling a process.</p> Source code in <code>src/retsu/core.py</code> <pre><code>def __init__(self, workers: int = 1) -&gt; None:\n    \"\"\"Initialize a process object.\"\"\"\n    _klass = self.__class__\n    queue_in_name = f\"{_klass.__module__}.{_klass.__qualname__}\"\n\n    self._client = redis.Redis(\n        **get_redis_queue_config(),  # type: ignore\n        decode_responses=False,\n    )\n    self.active = True\n    self.workers = workers\n    self.result: ResultProcessManager = create_result_task_manager()\n    self.queue_in = RedisRetsuQueue(queue_in_name)\n    self.processes: list[mp.Process] = []\n</code></pre>"},{"location":"api/core/#retsu.core.Process.prepare_process","title":"prepare_process","text":"<pre><code>prepare_process(data: dict[str, Any]) -&gt; None\n</code></pre> <p>Call the process with the necessary arguments.</p> Source code in <code>src/retsu/core.py</code> <pre><code>def prepare_process(self, data: dict[str, Any]) -&gt; None:\n    \"\"\"Call the process with the necessary arguments.\"\"\"\n    task_id = data.pop(\"task_id\")\n    self.result.metadata.update(task_id, \"status\", \"running\")\n    result = self.process(\n        *data[\"args\"],\n        task_id=task_id,\n        **data[\"kwargs\"],\n    )\n    self.result.save(task_id, result)\n    self.result.metadata.update(task_id, \"status\", \"completed\")\n</code></pre>"},{"location":"api/core/#retsu.core.Process.process","title":"process  <code>abstractmethod</code>","text":"<pre><code>process(*args, task_id: str, **kwargs) -&gt; Any\n</code></pre> <p>Define the process to be executed.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@abstractmethod\ndef process(self, *args, task_id: str, **kwargs) -&gt; Any:  # type: ignore\n    \"\"\"Define the process to be executed.\"\"\"\n    raise Exception(\"`process` not implemented yet.\")\n</code></pre>"},{"location":"api/core/#retsu.core.Process.request","title":"request","text":"<pre><code>request(*args, **kwargs) -&gt; str\n</code></pre> <p>Feed the queue with data from the request for the process.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef request(self, *args, **kwargs) -&gt; str:  # type: ignore\n    \"\"\"Feed the queue with data from the request for the process.\"\"\"\n    task_id = uuid4().hex\n    metadata = {\n        \"status\": \"starting\",\n        \"created_at\": datetime.now().isoformat(),\n        \"updated_at\": datetime.now().isoformat(),\n    }\n    self.result.create(task_id, metadata)\n    self.queue_in.put(\n        {\n            \"task_id\": task_id,\n            \"args\": args,\n            \"kwargs\": kwargs,\n        },\n    )\n    return task_id\n</code></pre>"},{"location":"api/core/#retsu.core.Process.run","title":"run","text":"<pre><code>run() -&gt; None\n</code></pre> <p>Run the process with data from the queue.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef run(self) -&gt; None:\n    \"\"\"Run the process with data from the queue.\"\"\"\n    while self.active:\n        data = self.queue_in.get()\n        self.prepare_process(data)\n</code></pre>"},{"location":"api/core/#retsu.core.Process.start","title":"start","text":"<pre><code>start() -&gt; None\n</code></pre> <p>Start processes.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef start(self) -&gt; None:\n    \"\"\"Start processes.\"\"\"\n    logging.info(f\"Starting process {self.__class__.__name__}\")\n    for _ in range(self.workers):\n        p = mp.Process(target=self.run)\n        p.start()\n        self.processes.append(p)\n</code></pre>"},{"location":"api/core/#retsu.core.Process.stop","title":"stop","text":"<pre><code>stop() -&gt; None\n</code></pre> <p>Stop processes.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef stop(self) -&gt; None:\n    \"\"\"Stop processes.\"\"\"\n    logging.info(f\"Stopping process {self.__class__.__name__}\")\n    if not self.active:\n        return\n\n    self.active = False\n\n    for i in range(self.workers):\n        p = self.processes[i]\n        p.terminate()\n        p.join(timeout=1)\n</code></pre>"},{"location":"api/core/#retsu.core.ProcessManager","title":"ProcessManager","text":"<pre><code>ProcessManager()\n</code></pre> <p>Manage tasks.</p> Source code in <code>src/retsu/core.py</code> <pre><code>def __init__(self) -&gt; None:\n    \"\"\"Create a list of retsu tasks.\"\"\"\n    self.tasks: dict[str, Process] = {}\n</code></pre>"},{"location":"api/core/#retsu.core.ProcessManager.get_process","title":"get_process","text":"<pre><code>get_process(name: str) -&gt; Optional[Process]\n</code></pre> <p>Get a process with the given name.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef get_process(self, name: str) -&gt; Optional[Process]:\n    \"\"\"Get a process with the given name.\"\"\"\n    return self.tasks.get(name)\n</code></pre>"},{"location":"api/core/#retsu.core.ProcessManager.setup","title":"setup","text":"<pre><code>setup() -&gt; None\n</code></pre> <p>Get a process with the given name.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef setup(self) -&gt; None:\n    \"\"\"Get a process with the given name.\"\"\"\n    if self.tasks:\n        return\n\n    warnings.warn(\n        \"`self.tasks` is empty. Override `setup` and create \"\n        \"`self.tasks` with the proper tasks.\"\n    )\n</code></pre>"},{"location":"api/core/#retsu.core.ProcessManager.start","title":"start","text":"<pre><code>start() -&gt; None\n</code></pre> <p>Start tasks.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef start(self) -&gt; None:\n    \"\"\"Start tasks.\"\"\"\n    if not self.tasks:\n        self.setup()\n\n    for task_name, process in self.tasks.items():\n        process.start()\n</code></pre>"},{"location":"api/core/#retsu.core.ProcessManager.stop","title":"stop","text":"<pre><code>stop() -&gt; None\n</code></pre> <p>Stop tasks.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef stop(self) -&gt; None:\n    \"\"\"Stop tasks.\"\"\"\n    if not self.tasks:\n        warnings.warn(\"There is no tasks to be stopped.\")\n        return\n\n    for task_name, process in self.tasks.items():\n        process.stop()\n</code></pre>"},{"location":"api/core/#retsu.core.SingleProcess","title":"SingleProcess","text":"<pre><code>SingleProcess(workers: int = 1)\n</code></pre> <p>               Bases: <code>Process</code></p> <p>Single Process class.</p> Source code in <code>src/retsu/core.py</code> <pre><code>def __init__(self, workers: int = 1) -&gt; None:\n    \"\"\"Initialize a serial process object.\"\"\"\n    if workers != 1:\n        warnings.warn(\n            \"SingleProcess should have just 1 worker. \"\n            \"Switching automatically to 1 ...\"\n        )\n        workers = 1\n    super().__init__(workers=workers)\n</code></pre>"},{"location":"api/core/#retsu.core.SingleProcess.prepare_process","title":"prepare_process","text":"<pre><code>prepare_process(data: dict[str, Any]) -&gt; None\n</code></pre> <p>Call the process with the necessary arguments.</p> Source code in <code>src/retsu/core.py</code> <pre><code>def prepare_process(self, data: dict[str, Any]) -&gt; None:\n    \"\"\"Call the process with the necessary arguments.\"\"\"\n    task_id = data.pop(\"task_id\")\n    self.result.metadata.update(task_id, \"status\", \"running\")\n    result = self.process(\n        *data[\"args\"],\n        task_id=task_id,\n        **data[\"kwargs\"],\n    )\n    self.result.save(task_id, result)\n    self.result.metadata.update(task_id, \"status\", \"completed\")\n</code></pre>"},{"location":"api/core/#retsu.core.SingleProcess.process","title":"process  <code>abstractmethod</code>","text":"<pre><code>process(*args, task_id: str, **kwargs) -&gt; Any\n</code></pre> <p>Define the process to be executed.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@abstractmethod\ndef process(self, *args, task_id: str, **kwargs) -&gt; Any:  # type: ignore\n    \"\"\"Define the process to be executed.\"\"\"\n    raise Exception(\"`process` not implemented yet.\")\n</code></pre>"},{"location":"api/core/#retsu.core.SingleProcess.request","title":"request","text":"<pre><code>request(*args, **kwargs) -&gt; str\n</code></pre> <p>Feed the queue with data from the request for the process.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef request(self, *args, **kwargs) -&gt; str:  # type: ignore\n    \"\"\"Feed the queue with data from the request for the process.\"\"\"\n    task_id = uuid4().hex\n    metadata = {\n        \"status\": \"starting\",\n        \"created_at\": datetime.now().isoformat(),\n        \"updated_at\": datetime.now().isoformat(),\n    }\n    self.result.create(task_id, metadata)\n    self.queue_in.put(\n        {\n            \"task_id\": task_id,\n            \"args\": args,\n            \"kwargs\": kwargs,\n        },\n    )\n    return task_id\n</code></pre>"},{"location":"api/core/#retsu.core.SingleProcess.run","title":"run","text":"<pre><code>run() -&gt; None\n</code></pre> <p>Run the process with data from the queue.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef run(self) -&gt; None:\n    \"\"\"Run the process with data from the queue.\"\"\"\n    while self.active:\n        data = self.queue_in.get()\n        self.prepare_process(data)\n</code></pre>"},{"location":"api/core/#retsu.core.SingleProcess.start","title":"start","text":"<pre><code>start() -&gt; None\n</code></pre> <p>Start processes.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef start(self) -&gt; None:\n    \"\"\"Start processes.\"\"\"\n    logging.info(f\"Starting process {self.__class__.__name__}\")\n    for _ in range(self.workers):\n        p = mp.Process(target=self.run)\n        p.start()\n        self.processes.append(p)\n</code></pre>"},{"location":"api/core/#retsu.core.SingleProcess.stop","title":"stop","text":"<pre><code>stop() -&gt; None\n</code></pre> <p>Stop processes.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef stop(self) -&gt; None:\n    \"\"\"Stop processes.\"\"\"\n    logging.info(f\"Stopping process {self.__class__.__name__}\")\n    if not self.active:\n        return\n\n    self.active = False\n\n    for i in range(self.workers):\n        p = self.processes[i]\n        p.terminate()\n        p.join(timeout=1)\n</code></pre>"},{"location":"api/queues/","title":"<code class=\"doc-symbol doc-symbol-nav doc-symbol-module\"></code> queues","text":""},{"location":"api/queues/#retsu.queues","title":"queues","text":"<p>Functions for handling queues and their configurations.</p>"},{"location":"api/queues/#retsu.queues.BaseRetsuQueue","title":"BaseRetsuQueue","text":"<pre><code>BaseRetsuQueue(name: str)\n</code></pre> <p>Base Queue class.</p> Source code in <code>src/retsu/queues.py</code> <pre><code>def __init__(self, name: str) -&gt; None:\n    \"\"\"Initialize BaseRetsuQueue.\"\"\"\n    self.name = name\n</code></pre>"},{"location":"api/queues/#retsu.queues.BaseRetsuQueue.get","title":"get  <code>abstractmethod</code>","text":"<pre><code>get() -&gt; Any\n</code></pre> <p>Get the next data from the queue.</p> Source code in <code>src/retsu/queues.py</code> <pre><code>@abstractmethod\ndef get(self) -&gt; Any:\n    \"\"\"Get the next data from the queue.\"\"\"\n    ...\n</code></pre>"},{"location":"api/queues/#retsu.queues.BaseRetsuQueue.put","title":"put  <code>abstractmethod</code>","text":"<pre><code>put(data: Any) -&gt; None\n</code></pre> <p>Put data into the end of the queue.</p> Source code in <code>src/retsu/queues.py</code> <pre><code>@abstractmethod\ndef put(self, data: Any) -&gt; None:\n    \"\"\"Put data into the end of the queue.\"\"\"\n    ...\n</code></pre>"},{"location":"api/queues/#retsu.queues.RedisRetsuQueue","title":"RedisRetsuQueue","text":"<pre><code>RedisRetsuQueue(name: str)\n</code></pre> <p>               Bases: <code>BaseRetsuQueue</code></p> <p>RedisRetsuQueue class.</p> Source code in <code>src/retsu/queues.py</code> <pre><code>def __init__(self, name: str) -&gt; None:\n    \"\"\"Initialize RedisRetsuQueue.\"\"\"\n    super().__init__(name)\n    self._client = redis.Redis(\n        **get_redis_queue_config(),  # type: ignore\n        decode_responses=False,\n    )\n</code></pre>"},{"location":"api/queues/#retsu.queues.RedisRetsuQueue.get","title":"get","text":"<pre><code>get() -&gt; Any\n</code></pre> <p>Get the next data from the queue.</p> Source code in <code>src/retsu/queues.py</code> <pre><code>def get(self) -&gt; Any:\n    \"\"\"Get the next data from the queue.\"\"\"\n    while True:\n        data = self._client.lpop(self.name)\n        if data is None:\n            time.sleep(0.1)\n            continue\n\n        return pickle.loads(data)  # type: ignore\n</code></pre>"},{"location":"api/queues/#retsu.queues.RedisRetsuQueue.put","title":"put","text":"<pre><code>put(data: Any) -&gt; None\n</code></pre> <p>Put data into the end of the queue.</p> Source code in <code>src/retsu/queues.py</code> <pre><code>def put(self, data: Any) -&gt; None:\n    \"\"\"Put data into the end of the queue.\"\"\"\n    self._client.rpush(self.name, pickle.dumps(data))\n</code></pre>"},{"location":"api/queues/#retsu.queues.get_redis_queue_config","title":"get_redis_queue_config","text":"<pre><code>get_redis_queue_config() -&gt; dict[str, Union[str, int]]\n</code></pre> <p>Get Redis Queue parameters from the environment.</p> Source code in <code>src/retsu/queues.py</code> <pre><code>@public\ndef get_redis_queue_config() -&gt; dict[str, Union[str, int]]:\n    \"\"\"Get Redis Queue parameters from the environment.\"\"\"\n    redis_host: str = os.getenv(\"RETSU_REDIS_HOST\", \"localhost\")\n    redis_port: int = int(os.getenv(\"RETSU_REDIS_PORT\", 6379))\n    redis_db: int = int(os.getenv(\"RETSU_REDIS_DB\", 0))\n\n    return {\"host\": redis_host, \"port\": redis_port, \"db\": redis_db}\n</code></pre>"},{"location":"api/references/","title":"Api references","text":""},{"location":"api/references/#retsu","title":"retsu","text":"<p>Retsu.</p>"},{"location":"api/references/#retsu.MultiProcess","title":"MultiProcess","text":"<pre><code>MultiProcess(workers: int = 1)\n</code></pre> <p>               Bases: <code>Process</code></p> <p>Initialize a parallel process object.</p> Source code in <code>src/retsu/core.py</code> <pre><code>def __init__(self, workers: int = 1) -&gt; None:\n    \"\"\"Initialize MultiProcess.\"\"\"\n    if workers &lt;= 1:\n        raise Exception(\"MultiProcess should have more than 1 worker.\")\n\n    super().__init__(workers=workers)\n</code></pre>"},{"location":"api/references/#retsu.MultiProcess.prepare_process","title":"prepare_process","text":"<pre><code>prepare_process(data: dict[str, Any]) -&gt; None\n</code></pre> <p>Call the process with the necessary arguments.</p> Source code in <code>src/retsu/core.py</code> <pre><code>def prepare_process(self, data: dict[str, Any]) -&gt; None:\n    \"\"\"Call the process with the necessary arguments.\"\"\"\n    task_id = data.pop(\"task_id\")\n    self.result.metadata.update(task_id, \"status\", \"running\")\n    result = self.process(\n        *data[\"args\"],\n        task_id=task_id,\n        **data[\"kwargs\"],\n    )\n    self.result.save(task_id, result)\n    self.result.metadata.update(task_id, \"status\", \"completed\")\n</code></pre>"},{"location":"api/references/#retsu.MultiProcess.process","title":"process  <code>abstractmethod</code>","text":"<pre><code>process(*args, task_id: str, **kwargs) -&gt; Any\n</code></pre> <p>Define the process to be executed.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@abstractmethod\ndef process(self, *args, task_id: str, **kwargs) -&gt; Any:  # type: ignore\n    \"\"\"Define the process to be executed.\"\"\"\n    raise Exception(\"`process` not implemented yet.\")\n</code></pre>"},{"location":"api/references/#retsu.MultiProcess.request","title":"request","text":"<pre><code>request(*args, **kwargs) -&gt; str\n</code></pre> <p>Feed the queue with data from the request for the process.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef request(self, *args, **kwargs) -&gt; str:  # type: ignore\n    \"\"\"Feed the queue with data from the request for the process.\"\"\"\n    task_id = uuid4().hex\n    metadata = {\n        \"status\": \"starting\",\n        \"created_at\": datetime.now().isoformat(),\n        \"updated_at\": datetime.now().isoformat(),\n    }\n    self.result.create(task_id, metadata)\n    self.queue_in.put(\n        {\n            \"task_id\": task_id,\n            \"args\": args,\n            \"kwargs\": kwargs,\n        },\n    )\n    return task_id\n</code></pre>"},{"location":"api/references/#retsu.MultiProcess.run","title":"run","text":"<pre><code>run() -&gt; None\n</code></pre> <p>Run the process with data from the queue.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef run(self) -&gt; None:\n    \"\"\"Run the process with data from the queue.\"\"\"\n    while self.active:\n        data = self.queue_in.get()\n        self.prepare_process(data)\n</code></pre>"},{"location":"api/references/#retsu.MultiProcess.start","title":"start","text":"<pre><code>start() -&gt; None\n</code></pre> <p>Start processes.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef start(self) -&gt; None:\n    \"\"\"Start processes.\"\"\"\n    logging.info(f\"Starting process {self.__class__.__name__}\")\n    for _ in range(self.workers):\n        p = mp.Process(target=self.run)\n        p.start()\n        self.processes.append(p)\n</code></pre>"},{"location":"api/references/#retsu.MultiProcess.stop","title":"stop","text":"<pre><code>stop() -&gt; None\n</code></pre> <p>Stop processes.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef stop(self) -&gt; None:\n    \"\"\"Stop processes.\"\"\"\n    logging.info(f\"Stopping process {self.__class__.__name__}\")\n    if not self.active:\n        return\n\n    self.active = False\n\n    for i in range(self.workers):\n        p = self.processes[i]\n        p.terminate()\n        p.join(timeout=1)\n</code></pre>"},{"location":"api/references/#retsu.Process","title":"Process","text":"<pre><code>Process(workers: int = 1)\n</code></pre> <p>Main class for handling a process.</p> Source code in <code>src/retsu/core.py</code> <pre><code>def __init__(self, workers: int = 1) -&gt; None:\n    \"\"\"Initialize a process object.\"\"\"\n    _klass = self.__class__\n    queue_in_name = f\"{_klass.__module__}.{_klass.__qualname__}\"\n\n    self._client = redis.Redis(\n        **get_redis_queue_config(),  # type: ignore\n        decode_responses=False,\n    )\n    self.active = True\n    self.workers = workers\n    self.result: ResultProcessManager = create_result_task_manager()\n    self.queue_in = RedisRetsuQueue(queue_in_name)\n    self.processes: list[mp.Process] = []\n</code></pre>"},{"location":"api/references/#retsu.Process.prepare_process","title":"prepare_process","text":"<pre><code>prepare_process(data: dict[str, Any]) -&gt; None\n</code></pre> <p>Call the process with the necessary arguments.</p> Source code in <code>src/retsu/core.py</code> <pre><code>def prepare_process(self, data: dict[str, Any]) -&gt; None:\n    \"\"\"Call the process with the necessary arguments.\"\"\"\n    task_id = data.pop(\"task_id\")\n    self.result.metadata.update(task_id, \"status\", \"running\")\n    result = self.process(\n        *data[\"args\"],\n        task_id=task_id,\n        **data[\"kwargs\"],\n    )\n    self.result.save(task_id, result)\n    self.result.metadata.update(task_id, \"status\", \"completed\")\n</code></pre>"},{"location":"api/references/#retsu.Process.process","title":"process  <code>abstractmethod</code>","text":"<pre><code>process(*args, task_id: str, **kwargs) -&gt; Any\n</code></pre> <p>Define the process to be executed.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@abstractmethod\ndef process(self, *args, task_id: str, **kwargs) -&gt; Any:  # type: ignore\n    \"\"\"Define the process to be executed.\"\"\"\n    raise Exception(\"`process` not implemented yet.\")\n</code></pre>"},{"location":"api/references/#retsu.Process.request","title":"request","text":"<pre><code>request(*args, **kwargs) -&gt; str\n</code></pre> <p>Feed the queue with data from the request for the process.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef request(self, *args, **kwargs) -&gt; str:  # type: ignore\n    \"\"\"Feed the queue with data from the request for the process.\"\"\"\n    task_id = uuid4().hex\n    metadata = {\n        \"status\": \"starting\",\n        \"created_at\": datetime.now().isoformat(),\n        \"updated_at\": datetime.now().isoformat(),\n    }\n    self.result.create(task_id, metadata)\n    self.queue_in.put(\n        {\n            \"task_id\": task_id,\n            \"args\": args,\n            \"kwargs\": kwargs,\n        },\n    )\n    return task_id\n</code></pre>"},{"location":"api/references/#retsu.Process.run","title":"run","text":"<pre><code>run() -&gt; None\n</code></pre> <p>Run the process with data from the queue.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef run(self) -&gt; None:\n    \"\"\"Run the process with data from the queue.\"\"\"\n    while self.active:\n        data = self.queue_in.get()\n        self.prepare_process(data)\n</code></pre>"},{"location":"api/references/#retsu.Process.start","title":"start","text":"<pre><code>start() -&gt; None\n</code></pre> <p>Start processes.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef start(self) -&gt; None:\n    \"\"\"Start processes.\"\"\"\n    logging.info(f\"Starting process {self.__class__.__name__}\")\n    for _ in range(self.workers):\n        p = mp.Process(target=self.run)\n        p.start()\n        self.processes.append(p)\n</code></pre>"},{"location":"api/references/#retsu.Process.stop","title":"stop","text":"<pre><code>stop() -&gt; None\n</code></pre> <p>Stop processes.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef stop(self) -&gt; None:\n    \"\"\"Stop processes.\"\"\"\n    logging.info(f\"Stopping process {self.__class__.__name__}\")\n    if not self.active:\n        return\n\n    self.active = False\n\n    for i in range(self.workers):\n        p = self.processes[i]\n        p.terminate()\n        p.join(timeout=1)\n</code></pre>"},{"location":"api/references/#retsu.ProcessManager","title":"ProcessManager","text":"<pre><code>ProcessManager()\n</code></pre> <p>Manage tasks.</p> Source code in <code>src/retsu/core.py</code> <pre><code>def __init__(self) -&gt; None:\n    \"\"\"Create a list of retsu tasks.\"\"\"\n    self.tasks: dict[str, Process] = {}\n</code></pre>"},{"location":"api/references/#retsu.ProcessManager.get_process","title":"get_process","text":"<pre><code>get_process(name: str) -&gt; Optional[Process]\n</code></pre> <p>Get a process with the given name.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef get_process(self, name: str) -&gt; Optional[Process]:\n    \"\"\"Get a process with the given name.\"\"\"\n    return self.tasks.get(name)\n</code></pre>"},{"location":"api/references/#retsu.ProcessManager.setup","title":"setup","text":"<pre><code>setup() -&gt; None\n</code></pre> <p>Get a process with the given name.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef setup(self) -&gt; None:\n    \"\"\"Get a process with the given name.\"\"\"\n    if self.tasks:\n        return\n\n    warnings.warn(\n        \"`self.tasks` is empty. Override `setup` and create \"\n        \"`self.tasks` with the proper tasks.\"\n    )\n</code></pre>"},{"location":"api/references/#retsu.ProcessManager.start","title":"start","text":"<pre><code>start() -&gt; None\n</code></pre> <p>Start tasks.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef start(self) -&gt; None:\n    \"\"\"Start tasks.\"\"\"\n    if not self.tasks:\n        self.setup()\n\n    for task_name, process in self.tasks.items():\n        process.start()\n</code></pre>"},{"location":"api/references/#retsu.ProcessManager.stop","title":"stop","text":"<pre><code>stop() -&gt; None\n</code></pre> <p>Stop tasks.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef stop(self) -&gt; None:\n    \"\"\"Stop tasks.\"\"\"\n    if not self.tasks:\n        warnings.warn(\"There is no tasks to be stopped.\")\n        return\n\n    for task_name, process in self.tasks.items():\n        process.stop()\n</code></pre>"},{"location":"api/references/#retsu.ResultProcessManager","title":"ResultProcessManager","text":"<pre><code>ResultProcessManager(host: str = 'localhost', port: int = 6379, db: int = 0)\n</code></pre> <p>Manage the result and metadata from tasks.</p> Source code in <code>src/retsu/results.py</code> <pre><code>def __init__(\n    self, host: str = \"localhost\", port: int = 6379, db: int = 0\n) -&gt; None:\n    \"\"\"Initialize ResultProcessManager.\"\"\"\n    self.client = redis.Redis(\n        host=host, port=port, db=db, decode_responses=False\n    )\n    self.metadata = ProcessMetadataManager(self.client)\n</code></pre>"},{"location":"api/references/#retsu.ResultProcessManager.create","title":"create","text":"<pre><code>create(task_id: str, metadata: dict[str, Any]) -&gt; None\n</code></pre> <p>Create a new metadata for a given process.</p> Source code in <code>src/retsu/results.py</code> <pre><code>def create(self, task_id: str, metadata: dict[str, Any]) -&gt; None:\n    \"\"\"Create a new metadata for a given process.\"\"\"\n    self.metadata.create(task_id, metadata)\n</code></pre>"},{"location":"api/references/#retsu.ResultProcessManager.get","title":"get","text":"<pre><code>get(task_id: str, timeout: Optional[int] = None) -&gt; Any\n</code></pre> <p>Get the result for a given process.</p> Source code in <code>src/retsu/results.py</code> <pre><code>def get(self, task_id: str, timeout: Optional[int] = None) -&gt; Any:\n    \"\"\"Get the result for a given process.\"\"\"\n    time_step = 0.5\n    if timeout:\n        timeout_countdown = float(timeout)\n        while self.status(task_id) != \"completed\":\n            sleep(time_step)\n            timeout_countdown -= time_step\n            if timeout_countdown &lt;= 0:\n                status = self.status(task_id)\n                raise Exception(\n                    \"Timeout(get): Process result is not ready yet. \"\n                    f\"Process status: {status}\"\n                )\n\n    elif self.status(task_id) != \"completed\":\n        status = self.status(task_id)\n        raise Exception(\n            \"Timeout(get): Process result is not ready yet. \"\n            f\"Process status: {status}\"\n        )\n    result = self.metadata.get(task_id, \"result\")\n    return pickle.loads(result) if result else result\n</code></pre>"},{"location":"api/references/#retsu.ResultProcessManager.load","title":"load","text":"<pre><code>load(task_id: str) -&gt; dict[str, Any]\n</code></pre> <p>Load the whole metadata for a given process.</p> Source code in <code>src/retsu/results.py</code> <pre><code>def load(self, task_id: str) -&gt; dict[str, Any]:\n    \"\"\"Load the whole metadata for a given process.\"\"\"\n    return self.metadata.get_all(task_id)\n</code></pre>"},{"location":"api/references/#retsu.ResultProcessManager.save","title":"save","text":"<pre><code>save(task_id: str, result: Any) -&gt; None\n</code></pre> <p>Save the result for a given process.</p> Source code in <code>src/retsu/results.py</code> <pre><code>def save(self, task_id: str, result: Any) -&gt; None:\n    \"\"\"Save the result for a given process.\"\"\"\n    self.metadata.update(task_id, \"result\", pickle.dumps(result))\n</code></pre>"},{"location":"api/references/#retsu.ResultProcessManager.status","title":"status","text":"<pre><code>status(task_id: str) -&gt; str\n</code></pre> <p>Get the status for a given process.</p> Source code in <code>src/retsu/results.py</code> <pre><code>def status(self, task_id: str) -&gt; str:\n    \"\"\"Get the status for a given process.\"\"\"\n    status = self.metadata.get(task_id, \"status\")\n    return status.decode(\"utf8\")\n</code></pre>"},{"location":"api/references/#retsu.SingleProcess","title":"SingleProcess","text":"<pre><code>SingleProcess(workers: int = 1)\n</code></pre> <p>               Bases: <code>Process</code></p> <p>Single Process class.</p> Source code in <code>src/retsu/core.py</code> <pre><code>def __init__(self, workers: int = 1) -&gt; None:\n    \"\"\"Initialize a serial process object.\"\"\"\n    if workers != 1:\n        warnings.warn(\n            \"SingleProcess should have just 1 worker. \"\n            \"Switching automatically to 1 ...\"\n        )\n        workers = 1\n    super().__init__(workers=workers)\n</code></pre>"},{"location":"api/references/#retsu.SingleProcess.prepare_process","title":"prepare_process","text":"<pre><code>prepare_process(data: dict[str, Any]) -&gt; None\n</code></pre> <p>Call the process with the necessary arguments.</p> Source code in <code>src/retsu/core.py</code> <pre><code>def prepare_process(self, data: dict[str, Any]) -&gt; None:\n    \"\"\"Call the process with the necessary arguments.\"\"\"\n    task_id = data.pop(\"task_id\")\n    self.result.metadata.update(task_id, \"status\", \"running\")\n    result = self.process(\n        *data[\"args\"],\n        task_id=task_id,\n        **data[\"kwargs\"],\n    )\n    self.result.save(task_id, result)\n    self.result.metadata.update(task_id, \"status\", \"completed\")\n</code></pre>"},{"location":"api/references/#retsu.SingleProcess.process","title":"process  <code>abstractmethod</code>","text":"<pre><code>process(*args, task_id: str, **kwargs) -&gt; Any\n</code></pre> <p>Define the process to be executed.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@abstractmethod\ndef process(self, *args, task_id: str, **kwargs) -&gt; Any:  # type: ignore\n    \"\"\"Define the process to be executed.\"\"\"\n    raise Exception(\"`process` not implemented yet.\")\n</code></pre>"},{"location":"api/references/#retsu.SingleProcess.request","title":"request","text":"<pre><code>request(*args, **kwargs) -&gt; str\n</code></pre> <p>Feed the queue with data from the request for the process.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef request(self, *args, **kwargs) -&gt; str:  # type: ignore\n    \"\"\"Feed the queue with data from the request for the process.\"\"\"\n    task_id = uuid4().hex\n    metadata = {\n        \"status\": \"starting\",\n        \"created_at\": datetime.now().isoformat(),\n        \"updated_at\": datetime.now().isoformat(),\n    }\n    self.result.create(task_id, metadata)\n    self.queue_in.put(\n        {\n            \"task_id\": task_id,\n            \"args\": args,\n            \"kwargs\": kwargs,\n        },\n    )\n    return task_id\n</code></pre>"},{"location":"api/references/#retsu.SingleProcess.run","title":"run","text":"<pre><code>run() -&gt; None\n</code></pre> <p>Run the process with data from the queue.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef run(self) -&gt; None:\n    \"\"\"Run the process with data from the queue.\"\"\"\n    while self.active:\n        data = self.queue_in.get()\n        self.prepare_process(data)\n</code></pre>"},{"location":"api/references/#retsu.SingleProcess.start","title":"start","text":"<pre><code>start() -&gt; None\n</code></pre> <p>Start processes.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef start(self) -&gt; None:\n    \"\"\"Start processes.\"\"\"\n    logging.info(f\"Starting process {self.__class__.__name__}\")\n    for _ in range(self.workers):\n        p = mp.Process(target=self.run)\n        p.start()\n        self.processes.append(p)\n</code></pre>"},{"location":"api/references/#retsu.SingleProcess.stop","title":"stop","text":"<pre><code>stop() -&gt; None\n</code></pre> <p>Stop processes.</p> Source code in <code>src/retsu/core.py</code> <pre><code>@public\ndef stop(self) -&gt; None:\n    \"\"\"Stop processes.\"\"\"\n    logging.info(f\"Stopping process {self.__class__.__name__}\")\n    if not self.active:\n        return\n\n    self.active = False\n\n    for i in range(self.workers):\n        p = self.processes[i]\n        p.terminate()\n        p.join(timeout=1)\n</code></pre>"},{"location":"api/references/#retsu.get_version","title":"get_version","text":"<pre><code>get_version() -&gt; str\n</code></pre> <p>Return the program version.</p> Source code in <code>src/retsu/__init__.py</code> <pre><code>def get_version() -&gt; str:\n    \"\"\"Return the program version.\"\"\"\n    try:\n        return importlib_metadata.version(__name__)\n    except importlib_metadata.PackageNotFoundError:  # pragma: no cover\n        return \"0.2.4\"  # semantic-release\n</code></pre>"},{"location":"api/results/","title":"<code class=\"doc-symbol doc-symbol-nav doc-symbol-module\"></code> results","text":""},{"location":"api/results/#retsu.results","title":"results","text":"<p>Retsu results classes.</p>"},{"location":"api/results/#retsu.results.ProcessMetadataManager","title":"ProcessMetadataManager","text":"<pre><code>ProcessMetadataManager(client: Redis)\n</code></pre> <p>Manage process metadata.</p> Source code in <code>src/retsu/results.py</code> <pre><code>def __init__(self, client: redis.Redis):\n    \"\"\"Initialize ProcessMetadataManager.\"\"\"\n    self.client = client\n    self.step = StepMetadataManager(self.client)\n</code></pre>"},{"location":"api/results/#retsu.results.ProcessMetadataManager.create","title":"create","text":"<pre><code>create(task_id: str, metadata: dict[str, Any]) -&gt; None\n</code></pre> <p>Create an initial metadata for given process.</p> Source code in <code>src/retsu/results.py</code> <pre><code>def create(self, task_id: str, metadata: dict[str, Any]) -&gt; None:\n    \"\"\"Create an initial metadata for given process.\"\"\"\n    self.client.hset(f\"process:{task_id}:metadata\", mapping=metadata)\n</code></pre>"},{"location":"api/results/#retsu.results.ProcessMetadataManager.get","title":"get","text":"<pre><code>get(task_id: str, attribute: str) -&gt; bytes\n</code></pre> <p>Get a specific metadata attribute for a given process.</p> Source code in <code>src/retsu/results.py</code> <pre><code>def get(self, task_id: str, attribute: str) -&gt; bytes:\n    \"\"\"Get a specific metadata attribute for a given process.\"\"\"\n    result = self.client.hget(f\"process:{task_id}:metadata\", attribute)\n    return cast(bytes, result)\n</code></pre>"},{"location":"api/results/#retsu.results.ProcessMetadataManager.get_all","title":"get_all","text":"<pre><code>get_all(task_id: str) -&gt; dict[str, bytes]\n</code></pre> <p>Get the entire metadata for a given process.</p> Source code in <code>src/retsu/results.py</code> <pre><code>def get_all(self, task_id: str) -&gt; dict[str, bytes]:\n    \"\"\"Get the entire metadata for a given process.\"\"\"\n    result = self.client.hgetall(f\"process:{task_id}:metadata\")\n    return cast(dict[str, bytes], result)\n</code></pre>"},{"location":"api/results/#retsu.results.ProcessMetadataManager.update","title":"update","text":"<pre><code>update(task_id: str, attribute: str, value: Any) -&gt; None\n</code></pre> <p>Update the value of given attribute for a given process.</p> Source code in <code>src/retsu/results.py</code> <pre><code>def update(self, task_id: str, attribute: str, value: Any) -&gt; None:\n    \"\"\"Update the value of given attribute for a given process.\"\"\"\n    self.client.hset(f\"process:{task_id}:metadata\", attribute, value)\n    self.client.hset(\n        f\"process:{task_id}:metadata\",\n        \"updated_at\",\n        datetime.now().isoformat(),\n    )\n</code></pre>"},{"location":"api/results/#retsu.results.ResultProcessManager","title":"ResultProcessManager","text":"<pre><code>ResultProcessManager(host: str = 'localhost', port: int = 6379, db: int = 0)\n</code></pre> <p>Manage the result and metadata from tasks.</p> Source code in <code>src/retsu/results.py</code> <pre><code>def __init__(\n    self, host: str = \"localhost\", port: int = 6379, db: int = 0\n) -&gt; None:\n    \"\"\"Initialize ResultProcessManager.\"\"\"\n    self.client = redis.Redis(\n        host=host, port=port, db=db, decode_responses=False\n    )\n    self.metadata = ProcessMetadataManager(self.client)\n</code></pre>"},{"location":"api/results/#retsu.results.ResultProcessManager.create","title":"create","text":"<pre><code>create(task_id: str, metadata: dict[str, Any]) -&gt; None\n</code></pre> <p>Create a new metadata for a given process.</p> Source code in <code>src/retsu/results.py</code> <pre><code>def create(self, task_id: str, metadata: dict[str, Any]) -&gt; None:\n    \"\"\"Create a new metadata for a given process.\"\"\"\n    self.metadata.create(task_id, metadata)\n</code></pre>"},{"location":"api/results/#retsu.results.ResultProcessManager.get","title":"get","text":"<pre><code>get(task_id: str, timeout: Optional[int] = None) -&gt; Any\n</code></pre> <p>Get the result for a given process.</p> Source code in <code>src/retsu/results.py</code> <pre><code>def get(self, task_id: str, timeout: Optional[int] = None) -&gt; Any:\n    \"\"\"Get the result for a given process.\"\"\"\n    time_step = 0.5\n    if timeout:\n        timeout_countdown = float(timeout)\n        while self.status(task_id) != \"completed\":\n            sleep(time_step)\n            timeout_countdown -= time_step\n            if timeout_countdown &lt;= 0:\n                status = self.status(task_id)\n                raise Exception(\n                    \"Timeout(get): Process result is not ready yet. \"\n                    f\"Process status: {status}\"\n                )\n\n    elif self.status(task_id) != \"completed\":\n        status = self.status(task_id)\n        raise Exception(\n            \"Timeout(get): Process result is not ready yet. \"\n            f\"Process status: {status}\"\n        )\n    result = self.metadata.get(task_id, \"result\")\n    return pickle.loads(result) if result else result\n</code></pre>"},{"location":"api/results/#retsu.results.ResultProcessManager.load","title":"load","text":"<pre><code>load(task_id: str) -&gt; dict[str, Any]\n</code></pre> <p>Load the whole metadata for a given process.</p> Source code in <code>src/retsu/results.py</code> <pre><code>def load(self, task_id: str) -&gt; dict[str, Any]:\n    \"\"\"Load the whole metadata for a given process.\"\"\"\n    return self.metadata.get_all(task_id)\n</code></pre>"},{"location":"api/results/#retsu.results.ResultProcessManager.save","title":"save","text":"<pre><code>save(task_id: str, result: Any) -&gt; None\n</code></pre> <p>Save the result for a given process.</p> Source code in <code>src/retsu/results.py</code> <pre><code>def save(self, task_id: str, result: Any) -&gt; None:\n    \"\"\"Save the result for a given process.\"\"\"\n    self.metadata.update(task_id, \"result\", pickle.dumps(result))\n</code></pre>"},{"location":"api/results/#retsu.results.ResultProcessManager.status","title":"status","text":"<pre><code>status(task_id: str) -&gt; str\n</code></pre> <p>Get the status for a given process.</p> Source code in <code>src/retsu/results.py</code> <pre><code>def status(self, task_id: str) -&gt; str:\n    \"\"\"Get the status for a given process.\"\"\"\n    status = self.metadata.get(task_id, \"status\")\n    return status.decode(\"utf8\")\n</code></pre>"},{"location":"api/results/#retsu.results.StepMetadataManager","title":"StepMetadataManager","text":"<pre><code>StepMetadataManager(redis_client: Redis)\n</code></pre> <p>Manage metadata for steps of a process.</p> Source code in <code>src/retsu/results.py</code> <pre><code>def __init__(self, redis_client: redis.Redis):\n    \"\"\"Initialize StepMetadataManager.\"\"\"\n    self.client = redis_client\n</code></pre>"},{"location":"api/results/#retsu.results.StepMetadataManager.create","title":"create","text":"<pre><code>create(task_id: str, step_id: str, metadata: dict[str, Any]) -&gt; None\n</code></pre> <p>Create an initial metadata for given process and step.</p> Source code in <code>src/retsu/results.py</code> <pre><code>def create(\n    self, task_id: str, step_id: str, metadata: dict[str, Any]\n) -&gt; None:\n    \"\"\"Create an initial metadata for given process and step.\"\"\"\n    self.client.hset(f\"process:{task_id}:step:{step_id}\", mapping=metadata)\n</code></pre>"},{"location":"api/results/#retsu.results.StepMetadataManager.get","title":"get","text":"<pre><code>get(task_id: str, step_id: str, attribute: str) -&gt; bytes\n</code></pre> <p>Get the value of a given attribute for a given process and step.</p> Source code in <code>src/retsu/results.py</code> <pre><code>def get(self, task_id: str, step_id: str, attribute: str) -&gt; bytes:\n    \"\"\"Get the value of a given attribute for a given process and step.\"\"\"\n    result = self.client.hget(\n        f\"process:{task_id}:step:{step_id}\", attribute\n    )\n    return cast(bytes, result)\n</code></pre>"},{"location":"api/results/#retsu.results.StepMetadataManager.get_all","title":"get_all","text":"<pre><code>get_all(task_id: str, step_id: str) -&gt; dict[str, bytes]\n</code></pre> <p>Get the whole metadata for a given process and step.</p> Source code in <code>src/retsu/results.py</code> <pre><code>def get_all(self, task_id: str, step_id: str) -&gt; dict[str, bytes]:\n    \"\"\"Get the whole metadata for a given process and step.\"\"\"\n    result = self.client.hgetall(f\"process:{task_id}:step:{step_id}\")\n    return cast(dict[str, bytes], result)\n</code></pre>"},{"location":"api/results/#retsu.results.StepMetadataManager.update","title":"update","text":"<pre><code>update(task_id: str, step_id: str, attribute: str, value: Any) -&gt; None\n</code></pre> <p>Update the value of given attribute for a given process and step.</p> Source code in <code>src/retsu/results.py</code> <pre><code>def update(\n    self, task_id: str, step_id: str, attribute: str, value: Any\n) -&gt; None:\n    \"\"\"Update the value of given attribute for a given process and step.\"\"\"\n    if attribute == \"status\" and value not in [\"started\", \"completed\"]:\n        raise Exception(\"Status should be started or completed.\")\n\n    self.client.hset(f\"process:{task_id}:step:{step_id}\", attribute, value)\n    self.client.hset(\n        f\"process:{task_id}:step:{step_id}\",\n        \"updated_at\",\n        datetime.now().isoformat(),\n    )\n</code></pre>"},{"location":"api/results/#retsu.results.create_result_task_manager","title":"create_result_task_manager","text":"<pre><code>create_result_task_manager() -&gt; ResultProcessManager\n</code></pre> <p>Create a ResultProcessManager with parameters from the environment.</p> Source code in <code>src/retsu/results.py</code> <pre><code>@public\ndef create_result_task_manager() -&gt; ResultProcessManager:\n    \"\"\"Create a ResultProcessManager with parameters from the environment.\"\"\"\n    return ResultProcessManager(**get_redis_queue_config())  # type: ignore\n</code></pre>"},{"location":"api/results/#retsu.results.track_step","title":"track_step","text":"<pre><code>track_step(task_metadata: ProcessMetadataManager) -&gt; Callable[..., Any]\n</code></pre> <p>Decorate a function with ProcessMetadataManager.</p> Source code in <code>src/retsu/results.py</code> <pre><code>@public\ndef track_step(task_metadata: ProcessMetadataManager) -&gt; Callable[..., Any]:\n    \"\"\"Decorate a function with ProcessMetadataManager.\"\"\"\n\n    def decorator(task_func: Callable[..., Any]) -&gt; Callable[..., Any]:\n        \"\"\"Return a decorator for the given process.\"\"\"\n\n        def wrapper(\n            *args: Unpack[Any], **kwargs: Unpack[dict[str, Any]]\n        ) -&gt; Any:\n            \"\"\"Wrap a function for registering the process metadata.\"\"\"\n            task_id = kwargs[\"task_id\"]\n            step_id = kwargs.get(\"step_id\", task_func.__name__)\n\n            step_metadata = task_metadata.step\n\n            step_metadata.update(task_id, step_id, \"status\", \"started\")\n            result = task_func(*args, **kwargs)\n            step_metadata.update(task_id, step_id, \"status\", \"completed\")\n            result_pickled = pickle.dumps(result)\n            step_metadata.update(task_id, step_id, \"result\", result_pickled)\n            return result\n\n        return wrapper\n\n    return decorator\n</code></pre>"},{"location":"api/plugins/","title":"Index","text":""},{"location":"api/plugins/#retsu.plugins","title":"plugins","text":"<p>Sub-package for plugins.</p>"},{"location":"api/plugins/django/","title":"<code class=\"doc-symbol doc-symbol-nav doc-symbol-module\"></code> django","text":""},{"location":"api/plugins/django/#retsu.plugins.django","title":"django","text":"<p>Plugin for integrating with django.</p>"},{"location":"api/plugins/django/#retsu.plugins.django.create_app_config","title":"create_app_config","text":"<pre><code>create_app_config(manager: ProcessManager, app_name: str = 'myapp') -&gt; Type[AppConfig]\n</code></pre> <p>Create a django app config class.</p> Source code in <code>src/retsu/plugins/django.py</code> <pre><code>def create_app_config(\n    manager: ProcessManager, app_name: str = \"myapp\"\n) -&gt; Type[AppConfig]:\n    \"\"\"Create a django app config class.\"\"\"\n\n    class RetsuAppConfig(AppConfig):\n        \"\"\"RetsuAppConfig class.\"\"\"\n\n        name = app_name\n\n        def ready(self) -&gt; None:\n            \"\"\"Start the process manager when the django app is ready.\"\"\"\n            manager.start()\n            request_finished.connect(self.stop_multiprocessing)\n\n        def stop_multiprocessing(self, **kwargs) -&gt; None:  # type: ignore\n            assert kwargs is not None\n            manager.stop()\n\n    return RetsuAppConfig\n</code></pre>"}]}